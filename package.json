{
  "name": "codeyam-counter",
  "version": "1.0.0",
  "private": true,
  "description": "CodeYam Counter iOS app — repo-level scripts.",
  "scripts": {
    "testflight": "gh workflow run testflight.yml --ref main",
    "testflight:watch": "gh workflow run testflight.yml --ref main && sleep 6 && gh run watch \"$(gh run list --workflow testflight.yml --limit 1 --json databaseId -q '.[0].databaseId')\""
  }
}
